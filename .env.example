# For E2E (npm run e2e) point these at a DISPOSABLE test Supabase project —
# the Maestro flows create real users (e2e_<timestamp>@followflow.test).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
